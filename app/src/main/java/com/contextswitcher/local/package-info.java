/// Running local processes (e.g. `powershell`) for desktop-integration
/// actions on the machine ContextSwitcher itself runs on (MADR 0008).
@NullMarked
package com.contextswitcher.local;

import org.jspecify.annotations.NullMarked;
