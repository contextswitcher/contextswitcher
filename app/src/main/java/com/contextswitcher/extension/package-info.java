/// Loopback WebSocket server and protocol for the browser extension
/// (MADR 0004): the extension connects to the app, authenticates with the
/// shared token, and executes tab commands (focus/open, close).
@NullMarked
package com.contextswitcher.extension;

import org.jspecify.annotations.NullMarked;
