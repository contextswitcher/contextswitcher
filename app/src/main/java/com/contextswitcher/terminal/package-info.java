/// Local terminal integration: focusing a Windows Terminal tab via UI
/// Automation, driven through `powershell` (MADR 0008).
@NullMarked
package com.contextswitcher.terminal;

import org.jspecify.annotations.NullMarked;
