# direnv (https://direnv.net): enters `shell.nix` on cd into the repo, so the
# JavaFX native libraries are on LD_LIBRARY_PATH without a `nix-shell` prefix.
#
# Only takes effect for those who have direnv installed and run `direnv allow`
# here once — it is inert for everyone else, including on Windows.
use nix
