<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The task repo sync (JGit over HTTPS) and SSH both open sockets; without
         it every connection fails with a permission error. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- The finish notification (TaskWatchService): the alert itself, and the
         foreground service that keeps polling while the app is in the
         background. specialUse: no predefined type covers "watch a remote
         session's status"; the app is sideloaded, not on Play. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- The update hint installs the android-dev pre-release's APK over the
         app (ApkInstaller); the second one lets Android 12+ skip the
         confirmation once ContextSwitcher installed itself. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.UPDATE_PACKAGES_WITHOUT_USER_ACTION" />

    <!-- The head-unit app's dictation (car/CarDictation): the car's microphone,
         and sight of the phone's speech recognition service. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".ContextSwitcherApplication"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".update.InstallResultReceiver"
            android:exported="false" />

        <service
            android:name=".watch.TaskWatchService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Polls a remote Claude Code session's status over SSH to notify when it finishes" />
        </service>

        <!-- The Android Auto head-unit app. IOT is the category closest to
             "talk to a remote session"; sideloaded, it shows only with
             Android Auto's "Unknown sources" developer setting. Car API
             level 5: CarAudioRecord and Header. -->
        <service
            android:name=".car.ContextSwitcherCarAppService"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.IOT" />
            </intent-filter>
        </service>
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="5" />

        <!-- Lends the camera app a file under attachments/ to write a photo
             into (QueuePage's Camera button); no CAMERA permission needed. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.files"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
